{% if resource_granted('luminaire_issue_view') %}
    {% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

    <div class="responsive-cell">
        <div class="box-type1">
            <div class="title">
                <span class="widget-title">{{ "luminaire.issue.placeholders.luminaire_user_assigned_issues_grid.title"|trans }}</span>
            </div>

            <div class="row-fluid">
                {{ dataGrid.renderGrid('assignee-issues-grid', {user_id: entity.id}) }}
            </div>
        </div>
    </div>
{% endif %}
