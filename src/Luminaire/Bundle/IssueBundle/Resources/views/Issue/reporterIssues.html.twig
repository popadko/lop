{#{% if resource_granted('orocrm_case_view') %}#}
    {% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

    <div class="responsive-cell">
        <div class="box-type1">
            <div class="title">
                <span class="widget-title">{{ "luminaire.issue.placeholders.orocrm_user_reported_issues_grid.title"|trans }}</span>
            </div>

            <div class="row-fluid">
                {{ dataGrid.renderGrid('reporter-issues-grid', {user_id: entity.id}) }}
            </div>
        </div>
    </div>
{#{% endif %}#}
